import 'package:get/get.dart';

/// Notification Service for reminders and cooling-off periods
class NotificationService extends GetxService {
  Future<void> scheduleCoolingOffReminder(String productId, DateTime triggerAt) async {
    // Schedule local notification
  }
}
