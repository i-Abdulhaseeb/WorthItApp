/// Global Application Constants
class AppConstants {
  AppConstants._();

  static const String appName = 'Worth It';
  static const String appVersion = '1.0.0';
  static const Duration timeoutDuration = Duration(seconds: 30);
}
